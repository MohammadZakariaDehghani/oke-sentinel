apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Not part of ../kustomization.yaml: these resources need an OCI Vault to read
# from and the External Secrets Operator CRDs to be installed, neither of which
# exists in the local and CI paths.
resources:
  - secretstore.yaml
  - externalsecrets.yaml
