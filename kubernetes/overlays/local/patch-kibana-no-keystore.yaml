# No OCI Vault in the local path, so there is no secret to load the keystore
# from. Kibana generates an ephemeral saved-object encryption key instead,
# which is fine for a cluster that is deleted at the end of the job.
- op: remove
  path: /spec/secureSettings
